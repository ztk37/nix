# `nix`

> My place to learn [nix](https://nixos.org/)

## Commands

```sh
nix repl

nix repl '<nixpkgs>'

nix repl <filename>

nix-instantiate --eval -E "with import <nixpkgs> {}; expr"

nix-shell
```
